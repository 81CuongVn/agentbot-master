language: node_js
node_js:
  - "stable"
cache: npm
install:
  - npm install
script:
  - npm run test
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on: 
    branch: master